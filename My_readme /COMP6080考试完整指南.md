# COMP6080 考试完整指南

> 包含：考试准备、终端命令、Git操作、GitHub/GitLab切换

---

# 📋 第一部分：考试准备

## 一、考试提供的资料

### 1.1 项目结构（往年情况）

**20T3-24T1（Create React App）：**
```
exam/
├── package.json          # 已配置好依赖
├── public/               # 静态资源
│   ├── index.html
│   └── favicon.ico
├── src/
│   ├── App.js            # 主应用文件（可能需要修改）
│   ├── App.css           # 样式文件
│   ├── index.js          # 入口文件
│   └── data/             # 可能提供的数据文件
│       ├── blankoData.js # 游戏数据
│       └── shrek/        # 图片资源
└── README.md             # 说明文档
```

**25T1（Vite）：**
- 可能需要自己初始化：`npm create vite@latest ./`
- 或者提供空仓库，自己搭建

### 1.2 已提供的依赖

**通常包含：**
- `react` 和 `react-dom`
- `react-router-dom` (路由)
- `react-scripts` (CRA) 或 `vite` (25T1)
- 测试库（通常不需要）

**可能需要自己安装：**
- 无！考试通常只需要基础React和路由

### 1.3 数据文件示例

**blankoData.js：**
```javascript
export const STRS = [
    'the fat cats',
    'larger frogs',
    'banana cakes',
    // ...
];
```

**API端点：**
- `https://cgi.cse.unsw.edu.au/~cs6080/raw/data/score.json`
- 或 `https://cs6080.web.cse.unsw.edu.au/raw/data/score.json`
- 格式：`{"score": 5}`

---

## 二、部署和启动命令

### 2.1 初始设置（考试开始后）

```bash
# 1. 克隆个人考试仓库
git clone <你的个人考试仓库链接>
cd exam

# 2. 检查是否已有package.json
ls package.json

# 情况A：已有package.json（20T3-24T1）
yarn install
# 或
npm install

# 情况B：空仓库（25T1或选择VanillaJS）
npm create vite@latest ./
# 选择：React + JavaScript
npm install
npm install react-router-dom  # 如果需要路由
```

### 2.2 开发模式启动

```bash
# Create React App (20T3-24T1)
yarn start
# 或
npm start
# 默认端口：http://localhost:3000

# Vite (25T1)
npm run dev
# 或
yarn dev
# 默认端口：http://localhost:5173
```

---

## 三、考试准备清单

### 3.1 考前准备

- [ ] **熟悉React基础**
  - useState, useEffect
  - 组件通信（props）
  - 事件处理

- [ ] **熟悉React Router**
  ```javascript
  import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';
  ```

- [ ] **熟悉localStorage**
  ```javascript
  localStorage.setItem('key', value);
  localStorage.getItem('key');
  localStorage.removeItem('key');
  ```

- [ ] **熟悉fetch API**
  ```javascript
  fetch(url)
    .then(res => res.json())
    .then(data => console.log(data));
  ```

- [ ] **熟悉CSS布局**
  - Flexbox
  - Grid
  - position: fixed
  - 媒体查询 @media

### 3.2 考试当天准备

- [ ] **环境检查**
  ```bash
  node --version    # 确保Node.js已安装
  yarn --version    # 或 npm --version
  git --version     # 确保Git可用
  ```

- [ ] **浏览器准备**
  - Chrome（最新版）
  - 开发者工具熟悉度
  - 响应式设计模式（F12 → 设备工具栏）

- [ ] **代码模板准备**
  - Dashboard模板（已保存在dashboard_template）
  - Header/Footer/Sidebar模板
  - localStorage工具函数

### 3.3 考试策略

**时间分配建议：**
1. **前30分钟**：阅读题目，理解需求，规划结构
2. **1-2小时**：完成Header/Footer/Sidebar + Dashboard
3. **2-3小时**：完成第一个游戏（通常最简单）
4. **3-4小时**：完成第二个游戏
5. **4-5小时**：完成第三个游戏（通常最复杂）
6. **最后30分钟**：测试响应式，修复bug，提交代码

**优先级：**
1. ✅ 功能实现（80%分数）
2. ✅ 响应式设计（20%分数）
3. ⚠️ 样式美化（不评分，但影响可用性）

---

## 四、常见问题和解决方案

### 4.1 启动问题

**问题：端口被占用**
```bash
# macOS/Linux
lsof -ti:3000 | xargs kill -9

# 或修改端口（CRA）
PORT=3001 yarn start
```

**问题：依赖安装失败**
```bash
# 清除缓存重试
rm -rf node_modules package-lock.json yarn.lock
yarn install
```

### 4.2 代码问题

**问题：路由不工作**
```javascript
// 确保在App.js中包裹BrowserRouter
<BrowserRouter>
  <Routes>
    <Route path="/" element={<Dashboard />} />
  </Routes>
</BrowserRouter>
```

**问题：localStorage不持久化**
```javascript
// 确保在useEffect中读取
useEffect(() => {
  const saved = localStorage.getItem('key');
  if (saved) {
    setValue(JSON.parse(saved));
  }
}, []);

// 确保在值改变时保存
useEffect(() => {
  localStorage.setItem('key', JSON.stringify(value));
}, [value]);
```

**问题：API请求失败**
```javascript
// 使用try-catch处理错误
try {
  const res = await fetch(url);
  const data = await res.json();
  // 处理数据
} catch (error) {
  console.error('API错误:', error);
  // 使用默认值
}
```

### 4.3 响应式问题

**问题：媒体查询不生效**
```css
/* 确保媒体查询顺序正确 */
/* 先写桌面，再写移动端 */
.header { /* 桌面样式 */ }

@media (max-width: 800px) {
  .header { /* 移动端样式 */ }
}
```

**问题：固定定位元素遮挡内容**
```css
/* 确保main body有padding-top */
.main-body {
  padding-top: 80px; /* header高度 */
  padding-bottom: 50px; /* footer高度 */
}
```

---

## 五、快速代码模板

### 5.1 App.js 基础结构

```javascript
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';
import { useState, useEffect } from 'react';
import './App.css';
import Dashboard from './Dashboard';
import Game1 from './Game1';
import Game2 from './Game2';
import Game3 from './Game3';

function App() {
  const [gamesWon, setGamesWon] = useState(0);

  useEffect(() => {
    const saved = localStorage.getItem('gamesWon');
    if (saved) {
      setGamesWon(parseInt(saved));
    } else {
      // 从API获取初始值
      fetch('https://cgi.cse.unsw.edu.au/~cs6080/raw/data/score.json')
        .then(res => res.json())
        .then(data => {
          setGamesWon(data.score);
          localStorage.setItem('gamesWon', data.score);
        });
    }
  }, []);

  return (
    <BrowserRouter>
      <Header />
      <Routes>
        <Route path="/" element={<Dashboard gamesWon={gamesWon} setGamesWon={setGamesWon} />} />
        <Route path="/game1" element={<Game1 setGamesWon={setGamesWon} />} />
        <Route path="/game2" element={<Game2 setGamesWon={setGamesWon} />} />
        <Route path="/game3" element={<Game3 setGamesWon={setGamesWon} />} />
      </Routes>
      <Footer />
    </BrowserRouter>
  );
}

export default App;
```

### 5.2 Header组件模板

```javascript
const Header = () => {
  return (
    <div className="header">
      <img src={logo} alt="logo" style={{width: 50, height: 50, margin: 15}} />
      <div className="nav">
        <Link to="/">
          <span className="long">Home |</span>
          <span className="short">H |</span>
        </Link>
        <Link to="/game1">
          <span className="long">Game1 |</span>
          <span className="short">G1 |</span>
        </Link>
        {/* 更多链接 */}
      </div>
    </div>
  );
};
```

### 5.3 localStorage工具函数

```javascript
// utils/storage.js
export const getStorage = (key, defaultValue) => {
  const saved = localStorage.getItem(key);
  return saved ? JSON.parse(saved) : defaultValue;
};

export const setStorage = (key, value) => {
  localStorage.setItem(key, JSON.stringify(value));
};

export const removeStorage = (key) => {
  localStorage.removeItem(key);
};
```

---

## 六、考试检查清单

### 提交前检查

- [ ] 所有路由正常工作
- [ ] localStorage数据持久化
- [ ] API请求有错误处理
- [ ] 响应式设计在三个尺寸下测试：
  - Desktop: 1800px × 800px (或1900px × 1000px)
  - Tablet: 1200px × 500px (或1200px × 800px)
  - Mobile: 600px × 500px (或400px × 800px)
- [ ] 所有游戏可以正常开始和结束
- [ ] Dashboard计数器正确更新
- [ ] Reset按钮工作正常
- [ ] 代码已提交到Git仓库
- [ ] 最后一次提交在master/main分支

### 功能检查

- [ ] Header/Footer/Sidebar符合要求
- [ ] Dashboard显示正确信息
- [ ] 游戏1功能完整
- [ ] 游戏2功能完整
- [ ] 游戏3功能完整
- [ ] 获胜后Dashboard更新
- [ ] 所有按钮和交互正常工作

---

# 🚀 第二部分：终端命令速查

## 一、项目启动流程

```bash
# 1. 克隆仓库
git clone <你的考试仓库链接>
cd exam

# 2. 安装依赖
yarn install
# 或
npm install

# 3. 启动开发服务器
yarn start          # Create React App
npm start           # Create React App
yarn dev            # Vite
npm run dev         # Vite
```

---

## 二、包管理命令

```bash
# 安装依赖
yarn install
npm install

# 安装新包
yarn add react-router-dom
npm install react-router-dom

# 查看已安装的包
yarn list
npm list
```

---

## 三、Git 命令（考试必用）

```bash
# 查看状态（最重要！）
git status

# 添加文件
git add .
git add <文件名>

# 提交代码
git commit -m "完成dashboard功能"

# 推送到远程
git push

# 如果lint错误阻止提交
git commit -m "message" --no-verify

# 查看提交历史
git log
# 按 q 退出

# 查看远程仓库地址
git remote -v
```

---

## 四、故障排除命令

```bash
# 端口被占用（macOS/Linux）
lsof -ti:3000                    # 查看3000端口
kill -9 $(lsof -ti:3000)         # 杀死进程

# 清除node_modules重新安装
rm -rf node_modules
rm -rf package-lock.json yarn.lock
yarn install

# 查看Node版本
node --version

# 查看npm/yarn版本
npm --version
yarn --version
```

---

## 五、文件操作命令

```bash
# 查看当前目录文件
ls
ls -la              # 显示隐藏文件

# 进入目录
cd <目录名>
cd ..              # 返回上一级

# 创建文件
touch <文件名>

# 创建目录
mkdir <目录名>

# 删除文件
rm <文件名>

# 删除目录
rm -rf <目录名>
```

---

## 六、网络相关

```bash
# 测试API是否可访问
curl https://cgi.cse.unsw.edu.au/~cs6080/raw/data/score.json

# 查看网络连接
ping google.com
```

---

## 七、常用组合命令

```bash
# 一键启动（如果端口被占用）
lsof -ti:3000 | xargs kill -9 && yarn start

# 清理并重新安装
rm -rf node_modules yarn.lock && yarn install

# 提交并推送
git add . && git commit -m "更新" && git push
```

---

## 八、考试当天流程

```bash
# 1. 克隆仓库
git clone <仓库链接>
cd exam

# 2. 检查package.json是否存在
cat package.json

# 3. 安装依赖
yarn install

# 4. 启动项目
yarn start

# 5. 开发过程中定期提交
git add .
git commit -m "完成功能X"
git push

# 6. 考试结束前最后提交
git add .
git commit -m "最终提交"
git push
```

---

## 九、紧急情况

```bash
# 如果一切都不工作了
rm -rf node_modules
rm package-lock.json yarn.lock
yarn install
yarn start

# 如果Git出问题
git status                    # 先看状态
git add .                     # 添加所有
git commit -m "保存" --no-verify  # 强制提交
git push                      # 推送
```

---

# 📤 第三部分：Git 上传步骤

## 一、标准三步流程（推荐）

```bash
# 1. 添加文件到暂存区
git add <文件夹名>
# 或添加所有文件
git add .

# 2. 提交到本地仓库
git commit -m "描述你做了什么"

# 3. 推送到远程
git push
```

### 示例：上传一个新文件夹

```bash
# 假设你创建了一个新文件夹 "新项目"
git add "新项目"
git commit -m "添加新项目文件夹"
git push
```

---

## 二、一行命令（快速）

```bash
# 添加、提交、推送一步完成
git add . && git commit -m "描述" && git push
```

### 示例：

```bash
git add . && git commit -m "添加新功能" && git push
```

---

## 三、完整流程示例

### 场景：上传一个名为 "exam-practice" 的新文件夹

```bash
# 步骤1：进入你的仓库目录
cd /Users/zihanw/6080

# 步骤2：查看当前状态（可选，但推荐）
git status

# 步骤3：添加新文件夹
git add "exam-practice"

# 步骤4：查看状态确认（可选）
git status

# 步骤5：提交
git commit -m "添加考试练习文件夹"

# 步骤6：推送到远程
git push

# 完成！刷新GitHub/GitLab页面即可看到
```

---

## 四、详细步骤说明

### 1. 查看状态（了解当前情况）

```bash
git status
```

**输出示例：**
```
Untracked files:
  exam-practice/
```

### 2. 添加文件到暂存区

```bash
# 添加单个文件夹
git add "exam-practice"

# 添加单个文件
git add "exam-practice/README.md"

# 添加所有新文件和修改
git add .

# 添加特定类型的文件
git add *.md          # 所有.md文件
git add *.js          # 所有.js文件
```

### 3. 确认已添加（可选但推荐）

```bash
git status
```

**输出示例：**
```
Changes to be committed:
  new file:   exam-practice/README.md
  new file:   exam-practice/App.js
```

### 4. 提交到本地仓库

```bash
git commit -m "描述性的提交信息"
```

**好的提交信息示例：**
```bash
git commit -m "添加考试练习项目"
git commit -m "完成dashboard功能"
git commit -m "修复响应式布局bug"
git commit -m "添加游戏1和游戏2"
```

**不好的提交信息（避免）：**
```bash
git commit -m "更新"        # ❌ 太模糊
git commit -m "fix"          # ❌ 不清楚
git commit -m "asdf"         # ❌ 无意义
```

### 5. 推送到远程

```bash
git push
```

**如果第一次推送新分支：**
```bash
git push -u origin main
```

---

## 五、常用场景

### 场景1：上传单个新文件夹

```bash
git add "新文件夹名"
git commit -m "添加新文件夹"
git push
```

### 场景2：上传多个文件夹

```bash
git add "文件夹1" "文件夹2" "文件夹3"
git commit -m "添加多个文件夹"
git push
```

### 场景3：上传所有更改（包括新文件和修改）

```bash
git add .
git commit -m "更新所有文件"
git push
```

### 场景4：只上传特定类型的文件

```bash
git add *.md              # 只上传所有.md文件
git commit -m "更新文档"
git push
```

---

## 六、注意事项

### 1. 文件夹名有空格

```bash
# 用引号包裹
git add "My readme folder"
```

### 2. 想忽略某些文件

创建 `.gitignore` 文件：
```
node_modules/
.DS_Store
*.log
```

### 3. 撤销操作

```bash
# 撤销暂存（还没commit）
git restore --staged <文件名>

# 撤销提交（还没push）
git reset HEAD~1

# 撤销推送（已经push，需要强制，谨慎使用）
git push --force
```

### 4. 查看提交历史

```bash
git log --oneline        # 简洁版
git log                  # 详细版
```

---

## 七、常见问题

### Q: 提示 "nothing to commit"
**A:** 说明没有新文件或修改，或者文件还没 `git add`

### Q: 提示 "Your branch is ahead of 'origin/main'"
**A:** 说明已commit但还没push，执行 `git push` 即可

### Q: 提示 "fatal: not a git repository"
**A:** 当前目录不是git仓库，需要先 `cd` 到仓库目录

### Q: 想修改上次的提交信息
**A:** `git commit --amend -m "新信息"`（如果还没push）

---

# 🔄 第四部分：GitHub 和 GitLab 切换

## 一、查看当前远程仓库

```bash
# 查看所有远程仓库
git remote -v

# 查看详细信息
git remote show origin
```

**当前状态：**
- `origin` → `https://github.com/wzh181/6080.git` (GitHub)

---

## 二、添加多个远程仓库（推荐方法）

### 方法一：保留GitHub，添加GitLab作为第二个远程

```bash
# 1. 查看当前远程
git remote -v

# 2. 添加学校GitLab作为第二个远程（命名为gitlab）
git remote add gitlab <你的学校GitLab仓库地址>

# 3. 验证
git remote -v
```

**示例：**
```bash
# 假设你的学校GitLab地址是：
# https://cgi.cse.unsw.edu.au/~cs6080/gitlabredir/25T1/exam

git remote add gitlab https://cgi.cse.unsw.edu.au/~cs6080/gitlabredir/25T1/exam
```

**结果：**
```
origin  https://github.com/wzh181/6080.git (fetch)
origin  https://github.com/wzh181/6080.git (push)
gitlab  https://cgi.cse.unsw.edu.au/~cs6080/... (fetch)
gitlab  https://cgi.cse.unsw.edu.au/~cs6080/... (push)
```

---

## 三、推送到不同的远程仓库

### 推送到GitHub（origin）

```bash
git push origin main
# 或简写（如果origin是默认）
git push
```

### 推送到学校GitLab（gitlab）

```bash
git push gitlab main
```

### 同时推送到两个仓库

```bash
git push origin main && git push gitlab main
```

---

## 四、切换默认远程仓库

### 方法一：修改origin指向（完全切换）

```bash
# 1. 查看当前origin
git remote -v

# 2. 删除当前origin
git remote remove origin

# 3. 添加新的origin（指向GitLab）
git remote add origin <GitLab地址>

# 4. 验证
git remote -v
```

**示例：切换到GitLab**
```bash
git remote remove origin
git remote add origin https://cgi.cse.unsw.edu.au/~cs6080/gitlabredir/25T1/exam
git push origin main
```

**切换回GitHub**
```bash
git remote remove origin
git remote add origin https://github.com/wzh181/6080.git
git push origin main
```

### 方法二：使用set-url（更简单，推荐）

```bash
# 修改origin的URL（不删除，直接改）
git remote set-url origin <新地址>
```

**示例：**
```bash
# 切换到GitLab
git remote set-url origin https://cgi.cse.unsw.edu.au/~cs6080/gitlabredir/25T1/exam

# 切换回GitHub
git remote set-url origin https://github.com/wzh181/6080.git
```

---

## 五、完整工作流程

### 场景1：平时用GitHub，考试时用GitLab

**平时开发（GitHub）：**
```bash
# 1. 确保origin指向GitHub
git remote set-url origin https://github.com/wzh181/6080.git

# 2. 正常开发流程
git add .
git commit -m "更新"
git push origin main
```

**考试时（GitLab）：**
```bash
# 1. 切换到GitLab
git remote set-url origin <学校GitLab地址>

# 2. 提交考试代码
git add .
git commit -m "考试提交"
git push origin main
```

### 场景2：同时维护两个仓库

**设置两个远程：**
```bash
# origin → GitHub
git remote set-url origin https://github.com/wzh181/6080.git

# gitlab → 学校GitLab
git remote add gitlab <学校GitLab地址>
```

**推送到GitHub：**
```bash
git push origin main
```

**推送到GitLab：**
```bash
git push gitlab main
```

**同时推送：**
```bash
git push origin main && git push gitlab main
```

---

## 六、考试专用流程

### 考试开始时的设置

```bash
# 1. 克隆学校考试仓库
git clone <学校GitLab考试仓库地址>
cd exam

# 2. 查看远程仓库
git remote -v

# 3. 确认是学校GitLab（应该是origin）
# 如果不是，修改：
git remote set-url origin <学校GitLab地址>

# 4. 开始开发...
```

### 考试期间的提交

```bash
# 每次完成功能后
git add .
git commit -m "完成功能X"
git push origin main
```

### 考试结束前

```bash
# 最后确认
git status
git log --oneline -5

# 最后推送
git push origin main

# 确认已推送
git status  # 应该显示 "Your branch is up to date"
```

---

## 七、常用命令速查

```bash
# ========== 查看远程 ==========
git remote -v                  # 查看所有远程
git remote show origin         # 查看origin详情

# ========== 添加远程 ==========
git remote add <名称> <地址>    # 添加新远程
git remote add gitlab <地址>    # 添加GitLab

# ========== 修改远程 ==========
git remote set-url origin <新地址>  # 修改origin地址（推荐）
git remote remove origin            # 删除origin

# ========== 推送 ==========
git push origin main           # 推送到origin
git push gitlab main           # 推送到gitlab
git push                       # 推送到默认（origin）

# ========== 拉取 ==========
git pull origin main           # 从origin拉取
git pull gitlab main           # 从gitlab拉取
```

---

## 八、最佳实践

### 1. 命名建议

```bash
# 推荐命名方式
origin   → GitHub（个人仓库）
gitlab   → 学校GitLab（考试仓库）
upstream → 原始仓库（如果有fork）
```

### 2. 考试时的工作流

```bash
# 考试开始
git remote set-url origin <学校GitLab地址>

# 开发过程中
git add .
git commit -m "功能描述"
git push origin main

# 考试结束
git push origin main  # 确保最后推送
```

### 3. 避免混淆

```bash
# 每次推送前检查
git remote -v

# 确认推送到哪里
git push origin main  # 明确指定
```

---

## 九、常见问题

### Q1: 如何知道当前推送到哪里？

```bash
git remote -v
git remote show origin
```

### Q2: 推送到错误的仓库怎么办？

```bash
# 如果还没push，没问题
# 如果已经push了，可以：
# 1. 在错误的仓库删除提交（如果允许）
# 2. 或者重新设置远程地址，推送到正确的地方
```

### Q3: 如何同时推送到两个仓库？

```bash
# 方法1：添加两个远程
git remote add gitlab <GitLab地址>
git push origin main && git push gitlab main

# 方法2：使用脚本
git push origin main; git push gitlab main
```

### Q4: 考试时如何确认推送到学校GitLab？

```bash
# 1. 检查远程地址
git remote -v

# 2. 确认地址是学校GitLab
# 应该类似：https://cgi.cse.unsw.edu.au/~cs6080/...

# 3. 推送后在学校GitLab网页确认
```

---

## 十、考试当天快速检查清单

```bash
# 1. 检查当前远程
git remote -v

# 2. 如果不是学校GitLab，切换
git remote set-url origin <学校GitLab地址>

# 3. 确认切换成功
git remote -v

# 4. 正常开发提交
git add .
git commit -m "描述"
git push origin main

# 5. 在学校GitLab网页确认已推送
```

---

# 🎯 快速参考卡

## Git 完整流程

```bash
# 1. 查看状态
git status

# 2. 添加文件
git add <文件/文件夹>
# 或
git add .

# 3. 提交
git commit -m "描述"

# 4. 推送
git push
# 或指定远程
git push origin main
```

## 一行命令

```bash
# 添加、提交、推送
git add . && git commit -m "更新" && git push

# 切换远程并推送
git remote set-url origin <新地址> && git push origin main
```

## 切换远程仓库

```bash
# 切换到GitLab
git remote set-url origin <GitLab地址>

# 切换回GitHub
git remote set-url origin https://github.com/wzh181/6080.git

# 添加第二个远程
git remote add gitlab <GitLab地址>
```

---

# 📚 参考资源

- React官方文档：https://react.dev
- React Router文档：https://reactrouter.com
- MDN localStorage：https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage
- CSS Flexbox指南：https://css-tricks.com/snippets/css/a-guide-to-flexbox/
- CSS Grid指南：https://css-tricks.com/snippets/css/complete-guide-grid/

---

**记住：add → commit → push 三步走！考试时一定要确认推送到学校GitLab！** ✅

**祝考试顺利！** 🎉

