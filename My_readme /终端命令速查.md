# 终端命令速查表（考试专用）

## 🚀 项目启动流程

```bash
# 1. 克隆仓库
git clone <你的考试仓库链接>
cd exam

# 2. 安装依赖
yarn install
# 或
npm install

# 3. 启动开发服务器
yarn start          # Create React App
npm start           # Create React App
yarn dev            # Vite
npm run dev         # Vite
```

---

## 📦 包管理命令

```bash
# 安装依赖
yarn install
npm install

# 安装新包
yarn add react-router-dom
npm install react-router-dom

# 查看已安装的包
yarn list
npm list
```

---

## 🔄 Git 命令（考试必用）

```bash
# 查看状态（最重要！）
git status

# 添加文件
git add .
git add <文件名>

# 提交代码
git commit -m "完成dashboard功能"

# 推送到远程
git push

# 如果lint错误阻止提交
git commit -m "message" --no-verify

# 查看提交历史
git log
# 按 q 退出

# 查看远程仓库地址
git remote -v
```

---

## 🛠️ 故障排除命令

```bash
# 端口被占用（macOS/Linux）
lsof -ti:3000                    # 查看3000端口
kill -9 $(lsof -ti:3000)         # 杀死进程

# 清除node_modules重新安装
rm -rf node_modules
rm -rf package-lock.json yarn.lock
yarn install

# 查看Node版本
node --version

# 查看npm/yarn版本
npm --version
yarn --version
```

---

## 📁 文件操作命令

```bash
# 查看当前目录文件
ls
ls -la              # 显示隐藏文件

# 进入目录
cd <目录名>
cd ..              # 返回上一级

# 创建文件
touch <文件名>

# 创建目录
mkdir <目录名>

# 删除文件
rm <文件名>

# 删除目录
rm -rf <目录名>
```

---

## 🌐 网络相关

```bash
# 测试API是否可访问
curl https://cgi.cse.unsw.edu.au/~cs6080/raw/data/score.json

# 查看网络连接
ping google.com
```

---

## 💡 常用组合命令

```bash
# 一键启动（如果端口被占用）
lsof -ti:3000 | xargs kill -9 && yarn start

# 清理并重新安装
rm -rf node_modules yarn.lock && yarn install

# 提交并推送
git add . && git commit -m "更新" && git push
```

---

## ⚡ 快速检查清单

```bash
# 考试开始前检查
node --version      # ✅ Node.js已安装
yarn --version       # ✅ Yarn已安装
git --version        # ✅ Git已安装
git status           # ✅ 仓库状态正常

# 启动后检查
# 浏览器打开 http://localhost:3000
# 检查控制台是否有错误
```

---

## 📝 考试当天流程

```bash
# 1. 克隆仓库
git clone <仓库链接>
cd exam

# 2. 检查package.json是否存在
cat package.json

# 3. 安装依赖
yarn install

# 4. 启动项目
yarn start

# 5. 开发过程中定期提交
git add .
git commit -m "完成功能X"
git push

# 6. 考试结束前最后提交
git add .
git commit -m "最终提交"
git push
```

---

## 🆘 紧急情况

```bash
# 如果一切都不工作了
rm -rf node_modules
rm package-lock.json yarn.lock
yarn install
yarn start

# 如果Git出问题
git status                    # 先看状态
git add .                     # 添加所有
git commit -m "保存" --no-verify  # 强制提交
git push                      # 推送
```

---

**提示：** 考试时把这个文件放在旁边，随时参考！ 📋

